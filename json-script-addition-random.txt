function playRandomProgram(playlist) {
    const audio = document.getElementById('myPlayer');

    // 1. Pick a random PROGRAM from your JSON array
    const randomIndex = Math.floor(Math.random() * playlist.length);
    const selectedTrack = playlist[randomIndex];

    // 2. Load the selected program
    audio.src = selectedTrack.url;

    // 3. Start the program at the beginning, not at a random point
    audio.onloadedmetadata = () => {
        audio.currentTime = 0;
        audio.play();
    };

    // 4. When the full program ends, play the outro.
    // The outro function should then call playNextTrack() when it finishes.
    audio.onended = () => {
        playOutro();
    };
}
